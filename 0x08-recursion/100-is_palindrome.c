#include "main.h"
#include <stdio.h>

int palhelper(char *s, char *t)
{
}
/**
 * is_palindrome - returns if a number is palindrome or not
 * @s: a character pointer
 *
 * Return: an integer
 */
int is_palindrome(char *s)
{
	if
}
