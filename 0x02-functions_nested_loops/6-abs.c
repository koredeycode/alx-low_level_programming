#include "main.h"
/**
 * _abs - returns the absolute value of the parameter
 *
 * @n: parameter of the int type
 *
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n * 1);
	}
	else
	{
		return (n * -1);
	}

}
