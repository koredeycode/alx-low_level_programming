#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - contains the if statement
 *
 * Return:always return 0
 *
*/
int main(void)
{
	int a = 122;

	while (a > 96)
	{
	putchar(a);
	a--;
	};
	putchar('\n');
	return (0);
}
