#include <stdio.h>
/**
 * main - print a program name followed by a new line
 * @argc: the argument count
 * @argv: the argument vector
 * Return: always 0
 */

int main(__attribute__((unused)) int argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
